# dbt MCP Server Configuration
# Copy this file to .env.dbt-mcp and update paths for your machine:
#   cp .env.dbt-mcp.example .env.dbt-mcp
#
# Prerequisites:
#   1. Install uv: brew install uv
#   2. Install dbt: uv tool install dbt-core==1.10.15 --with dbt-snowflake==1.10.3 --python python3.12
#   3. Find your paths:
#      - DBT_PROJECT_DIR: pwd (from this repo root)
#      - DBT_PATH: which dbt (or ~/.local/bin/dbt if installed via uv tool)

DBT_PROJECT_DIR=/path/to/this/repo
DBT_PATH=/path/to/dbt
DISABLE_SEMANTIC_LAYER=true
DISABLE_DISCOVERY=true
DISABLE_ADMIN_API=true
