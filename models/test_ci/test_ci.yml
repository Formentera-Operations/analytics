version: 2

models:
  - name: test_ci
    description: Test model to check if CI picks up modified models and runs the tests
    columns:
      - name: test_column
        description: Test column with hardcoded value
        data_tests:
          - not_null