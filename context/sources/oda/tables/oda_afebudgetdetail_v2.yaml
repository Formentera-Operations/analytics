# oda / afe_budgeting / ODA_AFEBUDGETDETAIL_V2
# 13 columns, 424,116 rows. Monthly AFE budget detail line items.
# Maps to stg_oda__afebudgetdetail_v2.
#
ODA_AFEBUDGETDETAIL_V2: AFE budget detail lines. Each row is a monthly budget amount for a specific AFE budget entry. Provides month-level granularity for budget vs actual tracking.

  # -- Identifiers --
  ID(txt) #Primary key (GUID)
  AFEBUDGETDETAIL_V2IDENTITY(int) #Numeric identity for the detail record
  AFEBUDGETENTRYID(txt) #FK to ODA_AFEBUDGETENTRY (not synced to Snowflake — 35K distinct entries)

  # -- Financial --
  AMOUNT(float) #Budget amount for this month
  MONTH(int) #Month number (1-12) this amount applies to

  # -- Audit --
  CREATEDATE(ts) #Record creation timestamp in ODA
  CREATEEVENTID(txt) #Event ID that created this record
  UPDATEDATE(ts) #Last update timestamp in ODA
  UPDATEEVENTID(txt) #Event ID of last update
  RECORDINSERTDATE(ts) #Estuary record insert timestamp
  RECORDUPDATEDATE(ts) #Estuary record update timestamp

  # -- CDC Metadata --
  FLOW_PUBLISHED_AT(ts) #Estuary CDC publication timestamp (maps to _loaded_at)
  FLOW_DOCUMENT(variant) #Full JSON document from Estuary CDC
