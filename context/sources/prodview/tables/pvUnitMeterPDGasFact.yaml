# prodview / meters / pvUnitMeterPDGasFact
# Source: context/sources/prodview/domains/meters.yaml
#
pvUnitMeterPDGasFact: Meter Factors +parent | Enter the date the meter factor comes into effect, a volume per volume factor, and the number of day
  DtTm(dt) #Effective Date
  Factor(dbl_m3pm3) #Factor
  DurValid(dbl_days) #Duration Between Calibrations
  DtTmNextCalc(dt_calc) #Next Calibration Due
