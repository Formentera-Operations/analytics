# prodview context index
# Structure: domains/ (relationships) + tables/ (column definitions)
#
# Domain files (load for cross-table context):
#   domains/admin.yaml — 14 tables
#   domains/allocations.yaml — 10 tables
#   domains/artificial_lift.yaml — 11 tables
#   domains/completions.yaml — 26 tables
#   domains/equipment.yaml — 8 tables
#   domains/facilities.yaml — 12 tables
#   domains/flow_network.yaml — 12 tables
#   domains/fluid_analysis.yaml — 21 tables
#   domains/meters.yaml — 18 tables
#   domains/reference.yaml — 27 tables
#   domains/routes.yaml — 8 tables
#   domains/tanks.yaml — 12 tables
#
# Per-table files (load for single-model work):
#   tables/{table_name}.yaml
#
# Total: 12 domains, 179 tables
